# Virtual environments
crm_sc_venv/
.venv/
venv/

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# OS
.DS_Store

# Jupyter / notebooks (kept local for now)
*.ipynb
*.nbconvert.ipynb

# Local data & generated artifacts (kept local; app reads via pipeline / Blob)
# anchored to repo root so they don't match webapp/public/data
/data/
/cs_data/
/reports/
/uv.lock

# Node / Next.js / Vercel (webapp)
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
.env
.env*.local

# NDVI PNG assets are small (~5 MB) and committed so Vercel serves them statically.
# (If they grow, switch to Vercel Blob and re-add: webapp/public/data/assets/)

# Internal planning docs (kept local)
/documentation/
